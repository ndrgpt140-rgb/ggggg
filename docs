# 🚀 دليل البدء السريع\n\n## الخطوة 1: التثبيت\n\n```bash\n# استنساخ المشروع\ngit clone https://github.com/ndrgpt140-rgb/ggggg.git\ncd ggggg\n\n# تثبيت الحزم\nnpm install\n```\n\n## الخطوة 2: إعداد المتغيرات البيئية\n\n```bash\n# نسخ ملف المثال\ncp .env.example .env.local\n```\n\nثم أضف البيانات التالية في `.env.local`:\n\n```env\n# Supabase\nNEXT_PUBLIC_SUPABASE_URL=your_url\nNEXT_PUBLIC_SUPABASE_ANON_KEY=your_key\nSUPABASE_SERVICE_ROLE_KEY=your_key\n\n# OpenAI\nOPENAI_API_KEY=your_key\n\n# NextAuth\nNEXTAUTH_SECRET=your_secret\nNEXTAUTH_URL=http://localhost:3000\n```\n\n## الخطوة 3: إنشاء قاعدة البيانات\n\nاذهب إلى [Supabase](https://supabase.com) وأنشئ جداول باستخدام SQL من `SETUP_AR.md`\n\n## الخطوة 4: البدء\n\n```bash\n# بدء خادم التطوير\nnpm run dev\n```\n\nافتح http://localhost:3000\n\n## 📋 الخطوات الأساسية\n\n### للموارد البشرية:\n1. اذهب إلى `/dashboard`\n2. أنشئ وظيفة جديدة\n3. أضف مرشحين\n4. انسخ رابط المقابلة وأرسله للمرشح\n5. اعرض التقييمات والتقارير\n\n### للمرشحين:\n1. افتح رابط المقابلة المرسل إليك\n2. اقرأ التعليمات\n3. وافق على الشروط\n4. أجب على الأسئلة بالصوت أو الكتابة\n5. أكمل المقابلة\n\n## 🆘 حل المشاكل\n\n### المشكلة: \"Cannot find module\"\n- الحل: `rm -rf node_modules && npm install`\n\n### المشكلة: API لا يعمل\n- الحل: تحقق من متغيرات البيئة في `.env.local`\n\n### المشكلة: الميكروفون لا يعمل\n- الحل: تأكد من السماح للمتصفح بالوصول للميكروفون\n\n---\n\nللمزيد من التفاصيل، اقرأ `SETUP_AR.md`\n",
  "path": "QUICKSTART_AR.md"
}, {"content": "# 🗺️ خريطة الطريق - نظام المقابلات الذكي\n\n## الإصدار 1.0 (المتاح الآن) ✅\n\n### الميزات الأساسية:\n- ✅ لوحة تحكم HR\n- ✅ إنشاء وظائف وإدارة المرشحين\n- ✅ توليد أسئلة ذكية\n- ✅ مقابلات تفاعلية (صوت + نص)\n- ✅ تقييم تلقائي\n- ✅ تقارير شاملة\n- ✅ رسائل تلقائية\n- ✅ دعم العربية والإنجليزية\n\n---\n\n## الإصدار 1.5 (Q3 2026) 🎯\n\n### المميزات المخطط إضافتها:\n- 📅 تقويم للمقابلات\n- 👥 إدارة فرق الموارد البشرية\n- 📊 إحصائيات وتحليلات متقدمة\n- 🔔 إشعارات فورية\n- 📧 تكامل مع بوابات التوظيف\n- 🤖 تحسينات الذكاء الاصطناعي\n\n---\n\n## الإصدار 2.0 (Q4 2026) 🚀\n\n### المميزات الجديدة:\n- 🎥 مقابلات فيديو مباشرة\n- 🌐 دعم لغات إضافية\n- 💼 ربط مع أنظمة HR الخارجية\n- 🔐 تقارير مشفرة\n- 📱 تطبيق جوال\n- 🧠 تحليل سلوكي متقدم\n\n---\n\n## المساهمة\n\nنرحب بالمساهمات! يرجى فتح issue أو pull request.\n\n---\n\n**آخر تحديث**: مايو 2026\n",
  "path": "ROADMAP_AR.md"
}, {"content": "# 🎨 دليل الأسلوب والمعايير\n\n## معايير الكود\n\n### TypeScript\n- استخدم دائماً types واضحة\n- تجنب `any` قدر الإمكان\n- استخدم interfaces للكائنات الكبيرة\n\n### React Components\n- استخدم functional components\n- استخدم hooks بدلاً من class components\n- افصل الحالة والمنطق عن الواجهة\n\n### تسمية الملفات\n- Components: `PascalCase.tsx` (مثل: `JobsList.tsx`)\n- Utilities: `camelCase.ts` (مثل: `helpers.ts`)\n- Pages: `lowercase` في المجلدات (مثل: `app/dashboard`)\n\n### الأنماط\n```tsx\n// ❌ خطأ\nconst MyComponent = () => {\n  const [state, setState] = useState()\n  return <div>{state}</div>\n}\n\n// ✅ صحيح\ninterface MyComponentProps {\n  title: string\n}\n\nexport default function MyComponent({ title }: MyComponentProps) {\n  const [state, setState] = useState<string>('')\n  return <div>{title}: {state}</div>\n}\n```\n\n## معايير التعليقات\n\n```tsx\n// استخدم تعليقات موجزة\n// شرح \"لماذا\" وليس \"ماذا\"\n\n// ❌ خطأ\n// increment the counter\ncounter++\n\n// ✅ صحيح\n// increment to track user interactions\ncounter++\n```\n\n## معايير Git\n\n### رسائل Commits\n```\n✨ feat: add new interview feature\n🐛 fix: resolve audio recording issue\n📝 docs: update setup instructions\n♻️ refactor: improve question generation\n🧪 test: add unit tests for API\n```\n\n## الأداء\n\n- استخدم `React.memo` للـ Components الكبيرة\n- قم بـ lazy loading للمكونات الثقيلة\n- قلل عدد API calls\n- استخدم debounce للـ searches\n\n## الأمان\n\n- لا تخزن secrets في الكود\n- استخدم environment variables\n- تحقق من inputs على الجانب الخادم\n- استخدم HTTPS في الإنتاج\n- sanitize جميع user inputs\n\n---\n\n**آخر تحديث**: مايو 2026\n",
  "path": "STYLE_GUIDE_AR.md"}\n